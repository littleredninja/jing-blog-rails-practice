<%= form_for(@favorite_post) do |f| %>
  <% if @favorite_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite_post.errors.count, "error") %> prohibited this favorite_post from being saved:</h2>

      <ul>
      <% @favorite_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.text_field :post_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
